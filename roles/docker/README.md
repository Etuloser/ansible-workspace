# Docker Role

Installs and configures Docker on Ubuntu/Debian systems.  

**Features:**

- Removes legacy Docker packages
- Adds Docker apt repository with official GPG key
- Installs `docker-ce` and related components

Variables are defined in `defaults/main.yml`.
